services:
  mcp-server:
    build:
      context: .
      dockerfile: docker/Dockerfile

    container_name: mcp-server
    restart: unless-stopped

    # Carrega TODAS as variáveis diretamente do arquivo .env
    env_file:
      - .env

    ports:
      - "3333:3333"

    dns:
      - 8.8.8.8
      - 1.1.1.1
