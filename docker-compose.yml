services:
  mcp-server:
    build:
      context: .
      dockerfile: docker/Dockerfile
    container_name: mcp-server
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - "3333:3333"
